import Head from 'next/head';
import Link from 'next/link';
import { Tab } from '@headlessui/react';
import Masonry from 'react-masonry-css';

const tabs = [
  {
    name: 'All',
  },
  {
    name: 'Ocean',
  },
  {
    name: 'Forest',
  },
];

export default function Home() {
  return (
    <div className="h-full bg-[url('/images/photography-bg.jpg')] bg-top bg-cover overflow-auto">
      <Head>
        <title>Photography Portfolio</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
      </Head>

      <header className="fixed top-0 w-full z-10 flex justify-between items-center h-[90px] px-6">
        <div className="hidden">hm</div>
        <div>Photography Portfolio</div>
        <Link
          href="#"
          className="rounded-3xl bg-white text-stone-600 px-5 py-3 hover:bg-opacity-90"
        >
          Get in touch
        </Link>
      </header>

      <main className="pt-[110px]">
        <div className="flex flex-col items-center h-full">
          <Tab.Group>
            <Tab.List className="flex items-center gap-4">
              {tabs.map((tab, index) => (
                <Tab key={`${tab}-${index}`} className="p-2">
                  {({ selected }) => (
                    <span
                      className={selected ? 'text-white' : 'text-stone-600'}
                    >
                      {tab.name}
                    </span>
                  )}
                </Tab>
              ))}
            </Tab.List>
            <Tab.Panels className="h-full max-w-[900px] w-full p-2 sm:p-4 my-6">
              <Tab.Panel>
                <Masonry
                  breakpointCols={3}
                  className="flex gap-2"
                  columnClassName=""
                >
                  <img src="/images/ocean-1.jpeg" alt="" className="my-2" />
                  <img src="/images/ocean-2.jpeg" alt="" className="my-2" />
                  <img src="/images/ocean-3.jpeg" alt="" className="my-2" />
                  <img src="/images/ocean-4.jpeg" alt="" className="my-2" />
                  <img src="/images/ocean-5.jpeg" alt="" className="my-2" />
                </Masonry>
              </Tab.Panel>
            </Tab.Panels>
          </Tab.Group>
        </div>
      </main>

      <footer className="h-[60px] flex justify-center items-center">
        <p>Photography Portfolio</p>
      </footer>
    </div>
  );
}
